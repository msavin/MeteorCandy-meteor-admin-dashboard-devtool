defaults = {
	// default tab for opening
	// sample options: Data, Search, Recents
	tab: "Recents",

	// default tab for account stats
	// it must mirror stats[].name as in server.js
	dataTab: "Total Accounts",

}
export { defaults }